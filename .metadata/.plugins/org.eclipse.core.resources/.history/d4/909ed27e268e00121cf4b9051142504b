attribute vec3 position;
attribute float color;
attribute float x;
attribute float y;
attribute float z;

void main(void) {
    gl_Position = vec4(position, color);
    // transmit vertex color into fragment shader  
    //fragmentColor = color; 
}