import java.nio.IntBuffer;

public class GraphicsObject {
	protected IntBuffer myColorID;
	private static long lastID = 0;
	
	public GraphicsObject() {
		this.myColorID = IntBuffer.allocate(4);
		this.myColorID.put((int)(lastID % 256));
		this.myColorID.put((int)(lastID/256 % 256));
		this.myColorID.put((int)(lastID/65536 % 256));
		lastID++;
	}
	
	@Override
	public String toString() {
		return "(" + myColorID[0] + ", " + myColorID[1] + ", " +  myColorID[2] + ")";
	}
}
