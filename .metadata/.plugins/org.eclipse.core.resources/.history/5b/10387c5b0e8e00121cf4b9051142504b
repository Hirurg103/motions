
uniform mat4 projectionMatrix;

attribute vec3 color1;
//attribute vec3 position;

//vec3 fragmentColor;

void main(void) {
    //
    gl_Position = vec4(0.0, 0.0, 0.0, 1.0);
    // transmit vertex color into fragment shader  
    //fragmentColor = color; 
}